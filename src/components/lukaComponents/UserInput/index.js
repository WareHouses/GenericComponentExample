import React from 'react'import { Input , Label , Icon , Popup } from 'semantic-ui-react';import TextSegment from '../TextSegment';import { BlueGrey900 } from '../Colors';import { errorStyle } from '../Styles';class UserInput extends React.Component{  constructor(props){    super(props);    this.state={}  }  render(){    const { meta : { touched , error} , helpTitle, helpText} = this.props    return(      <div style={{paddingTop:this.props.padded?"30px":''}} >        <TextSegment basic text={<h5>{this.props.title}</h5>} subtextColor={BlueGrey900}/>        <Input          error={( touched && error )? true : false}          {...this.props}          {...this.props.input}        />        { (helpText || helpTitle ) && <Popup                    trigger={<Icon name="help circle outline" color="blue"/>}                    header={(helpTitle)?helpTitle:""}                    content={(helpText)?helpText:""}                    size="mini"                    on="hover"                    position="top left"                  />                }        {( touched && error )?<span style={errorStyle}><Label basic color="red" pointing="left">{error}</Label></span>:null}      </div>      );  }}export default UserInput;
