import React from 'react'import Start from '../Start'import i18n from '../../i18n'class Contact extends React.Component{  constructor(props){    super(props);    this.state={}  }  commaToJump = (string) => {    return string.split('\n').map(function(item, key) {      return (        <span key={key}>          {item}          <br/>        </span>      )    })  }  render(){    return(      <Start title={this.commaToJump(i18n.t('contact.info'))}/>    );  }}export default Contact;
